#include<iostream>
#include<cmath>
#include<iomanip>
using namespace std;
int main()
{
	int a,b,c;
	double p,S;
	cin>>a;cin.get();
	cin>>b;cin.get();
	cin>>c;
	p=(a+b+c)*(1.0)/2;
	S=sqrt(p*(p-a)*(p-b)*(p-c));
	cout<<setiosflags(ios::fixed)<<setprecision(2)<<S;
	return 0;
 } 
